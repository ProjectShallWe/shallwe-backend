spring:
  profiles:
    group:
      local:
        - local
        - redis
        - s3
      dev:
        - dev
        - rds
        - redis
        - s3
      test:
        - test
        - rds
        - redis
        - s3
---
spring:
  config:
    activate:
      on-profile: local
---
spring:
  config:
    activate:
      on-profile: dev
---
spring:
  config:
    activate:
      on-profile: test